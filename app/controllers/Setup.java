package controllers;

import play.mvc.Controller;
import play.mvc.Result;

public class Setup extends Controller {

	public Result index() {
		/**
		 * TODO: Do not run if setup already completed.
		 * STEPS:
		 * Create admin account.
		 *
		 */

		return ok();
	}
}
